spring:
  datasource:
    url: jdbc:mysql://localhost:3306/social_shoot?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Kun0802
    driver-class-name: com.mysql.cj.jdbc.Driver
  mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.socialshoot.admin.entity
    configuration:
      map-underscore-to-camel-case: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8086
  servlet:
    context-path: /admin

jwt:
  secret: social_shoot_admin_secret_key
  expiration: 86400000
